/**
 * Utility functions barrel export
 *
 * This barrel exports all utility modules for clean imports across the app.
 * As the project grows, add new utility modules here.
 *
 * Example usage:
 *   import { cn } from '@/lib/utils';  // Tailwind class merging
 *   import { formatDate } from '@/utils';  // Date formatting (when implemented)
 */

// Re-export commonly used utilities from lib
export { cn } from '@/lib/utils';

// Placeholder for future utility modules:
// export { formatDate, formatCurrency } from './format';
// export { validateEmail } from './validation';
// export { slugify, truncate } from './string';
// export { groupBy, sortBy } from './array';
// export { clamp, randomInt } from './number';

