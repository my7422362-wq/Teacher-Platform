/**
 * Features barrel export
 *
 * Each feature module encapsulates a distinct domain capability.
 * Feature modules should be self-contained with their own:
 *   - components/    - UI components specific to this feature
 *   - hooks/         - hooks specific to this feature
 *   - services/      - API service calls specific to this feature
 *   - types/         - TypeScript types specific to this feature
 *   - constants/     - constants specific to this feature
 *   - pages/         - page components (if feature has dedicated pages)
 *
 * Import pattern:
 *   import { useAuth } from '@/features/auth';
 *   import { CourseCard } from '@/features/courses';
 */

export {};

